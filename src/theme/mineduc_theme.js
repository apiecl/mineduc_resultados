export const getColors = (darkTheme) => {
  return darkTheme === true
    ? { red: "#FAD8E1", blue: "#D3DEF2" }
    : { red: "#D34157", blue: "#0F69B4" };
};

export const mineducNivoTheme = {
  fontSize: 14,
};
